## Check to see what Git has your name configured as
    git config user.name

## Configure your Git username and email
    git config --global user.name "Name Here"
    git config --global user.email "Email Here"